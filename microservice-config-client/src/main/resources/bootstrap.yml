spring:
  cloud:
    config:
      name: microservice-config-client
      profile: dev   #本次访问的配置项
      label: master
      uri: http://192.168.5.211:10003  #本微服务启动后先去找10003号服务，通过SpringCloudConfig获取GitHub的服务地址